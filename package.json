{
  "scripts": {
    "json": "json-server --watch db.json --port 3000",
    "core": "npm run json & mocha --timeout 10000 test/core_test.js",
    "adv": "npm run json & mocha --timeout 10000 test/advanced_test.js"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.0.4",
    "mocha-multi": "^1.1.3",
    "puppeteer": "^1.2.0"
  }
}
